# NEXT

## Immediate Next Actions

1. **Merge** the control-tower PR that adds `.github/repo-factory/requests/2026-05-14-eid0-twistile.json` (and project files) to **`main`**, then confirm the **Repo Factory** workflow run succeeds and `https://github.com/eido740/eid0-twistile` exists.
2. **Clone** `eid0-twistile`, open it as the **workspace root**, and start a **new agent/session** there—point it at this folder’s `README.md` / `PLAN.md` for intent (or paste a one-line bridge: “Twistile → eid0.com/mosaic Rubik mosaic generator”).
3. **Decide v1 physical model** in that session: one cube face per grid cell vs richer mapping—then lock data structures and UI copy accordingly.
