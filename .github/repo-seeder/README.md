# Repo Seeder Ledger

## Directories
- `requests/` contains pending seed requests.
- `processed/` contains archived request results.

## Workflow
- Automation is handled by `.github/workflows/repo-seeder.yml`.
- It runs on `main` when seeding request files change.

## Current profile support
- `eid0-site-bootstrap-v1`
