name: Control Tower Deploy Guard (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "Deploy is intentionally disabled in the control-tower repo."
          echo "Use the dedicated site execution repo (eid0-site) for production publishing."
